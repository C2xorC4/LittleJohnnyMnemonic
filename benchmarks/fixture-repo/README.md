# Benchmark Fixture Repo

Neutral working directory for benchmark prompts. Contains no answers — tasks
resolve against `fixture-vault/Memory/` only.

Read-only evaluation: no deploy scripts, no git operations, no file mutations
required for grading.